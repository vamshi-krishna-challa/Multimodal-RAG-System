# Notes

This repository was refactored from early multimodal and RAG demo scripts into a GitHub-ready project.

Security note: do not commit API keys, local vector databases, uploaded files, or `.env` files.
